tup = 'Hello'
print(len(tup))
#the len function is returning to us the number of characters in the string 'Hello'

tup = 'Hello',
print(len(tup))
#the len function is returning to us the number of elements in the tuple - the trailing comma denotes that 'Hello' as a tuple.

